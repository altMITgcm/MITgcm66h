Example: Subtropical North Atlantic Subduction area
===================================================
- with KPP & shortwave heating
- no GMRedi
- forward/adjoint run
- optimization cost function

To apply the changes to the default configuration of MITgcmUV:
  cd bin
  ln -s ../verification/natl_box_adjoint/code/.genmakerc .
  ln -s ../verification/natl_box_adjoint/code/*.h .

Configure and compile the code:
  ../tools/genmake -makefile
  make depend
  make small_f

Generate the adjoint code:
  cd ../adjoint
  make adtaf
  make adchange

Go back and compile the code:
  cd ../bin
  make

To run:
  cd ../exe
  cp ../verification/natl_box_adjoint/input/* .
  mitgcmuv >&! output.txt

There is comparison output in:
  diff output.txt ../verification/natl_box_adjoint/results/output.txt | more

Comments:
The input data is real*8.
